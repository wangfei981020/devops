---
apiVersion: v1
kind: Secret
metadata:
  name: redis-pc-secret
  namespace: sso
type: Opaque
stringData:
  password: "PcRedis@2026#Secure"

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: redis-pc-config
  namespace: sso
data:
  redis.conf: |
    bind 0.0.0.0
    port 6379
    requirepass PcRedis@2026#Secure
    maxmemory 128mb
    maxmemory-policy allkeys-lru
    appendonly yes
    appendfsync everysec
    tcp-keepalive 300
    timeout 0
    databases 16

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-pc
  namespace: sso
  labels:
    app: redis-pc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-pc
  template:
    metadata:
      labels:
        app: redis-pc
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
        command:
        - redis-server
        - /etc/redis/redis.conf
        volumeMounts:
        - name: redis-config
          mountPath: /etc/redis
        - name: redis-data
          mountPath: /data
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          exec:
            command:
            - redis-cli
            - -a
            - "PcRedis@2026#Secure"
            - ping
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - redis-cli
            - -a
            - "PcRedis@2026#Secure"
            - ping
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: redis-config
        configMap:
          name: redis-pc-config
      - name: redis-data
        emptyDir: {}

---
apiVersion: v1
kind: Service
metadata:
  name: redis-pc
  namespace: sso
  labels:
    app: redis-pc
spec:
  selector:
    app: redis-pc
  ports:
  - port: 6379
    targetPort: 6379
  type: ClusterIP
