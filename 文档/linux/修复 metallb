📝 Kubernetes 集群 MetalLB 修复文档（Calico + MetalLB）

适用于：

Kubernetes v1.34

Calico CNI

MetalLB L2 模式

环境为 VMware / 物理机二层网络

1. 问题背景

在 Kubernetes 集群中安装 MetalLB 后，LoadBalancer 类型的 Service 无法正常分配 External IP 或无法通过 External IP 访问服务。

排查发现：

Calico Node 认证失败

/etc/cni/net.d/calico-kubeconfig 中的 token 失效

calico-node Pod 一直 CrashLoopBackOff 或无法启动

MetalLB Speaker 正常运行，但由于 CNI 不正常导致 External-IP 不可访问

因此需修复 Calico Node 认证并重新让 MetalLB 正常运行。

2. 修复步骤
2.1 查看 Calico Node ServiceAccount 信息
kubectl -n calico-system get sa calico-node -o yaml


确认集群中确实存在 calico-node ServiceAccount。

2.2 创建新的 token

K8s v1.24+ 不自动创建 Secret，需要手动生成 token：

kubectl -n calico-system create token calico-node


输出类似：

eyJhbGc...


保存这个 Token。

2.3 备份旧 CNI 配置

在所有节点执行：

cp /etc/cni/net.d/calico-kubeconfig /etc/cni/net.d/calico-kubeconfig.bak

2.4 替换 calico-kubeconfig 中的 Token

编辑：

vi /etc/cni/net.d/calico-kubeconfig


找到字段：

token: eyJ...


替换为你刚创建的新 Token。

保存退出。

2.5 重启 kubelet（每个节点都执行）
systemctl restart kubelet


等待几秒。

2.6 验证 Calico 是否恢复正常
kubectl get pods -n calico-system -o wide


应看到：

calico-node-xxxx   1/1   Running


说明 Token 已生效。

3. 验证 MetalLB 是否恢复正常
3.1 检查 MetalLB 组件
kubectl get pod -n metallb-system -o wide


应该全部 Running：

controller-xxxx   Running
speaker-xxxx      Running

3.2 创建 Nginx 测试服务
kubectl create deployment nginx --image=nginx
kubectl expose deployment nginx --port=80 --type=LoadBalancer

3.3 查看是否获得 External IP
kubectl get svc nginx


结果示例：

NAME    TYPE           CLUSTER-IP      EXTERNAL-IP       PORT(S)
nginx   LoadBalancer   10.109.119.30   192.168.248.210   80:31130/TCP


说明 MetalLB 已开始正常分配 IP。

3.4 访问服务验证
curl http://192.168.248.210


返回 nginx 默认页面：

Welcome to nginx!


🎉 MetalLB 完全恢复正常。

4. 最终集群状态检查（成功）
calico-node    全部 Running
metallb        controller/speaker 全部 Running
nginx LB       External-IP 可访问


示例输出：

kubectl get pods -n metallb-system
NAME                          READY   STATUS    AGE
controller-6599cd9c46-hp4l9   1/1     Running   16m
speaker-6hnxk                 1/1     Running   16m
speaker-l8snf                 1/1     Running   16m
speaker-rjdn6                 1/1     Running   16m

kubectl get svc nginx
nginx   LoadBalancer   10.109.119.30   192.168.248.210

curl http://192.168.248.210
Welcome to nginx!

5. 修复结果

✔ Calico token 修复完成
✔ calico-node 全部恢复正常
✔ kubelet 恢复正常鉴权
✔ MetalLB speaker 正常广播 ARP
✔ LoadBalancer 外网访问恢复成功

你已经成功恢复了整个网络栈。